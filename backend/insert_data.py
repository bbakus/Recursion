"""Helpers for seeding or syncing authored content into backend stores.

For the first slice, JSON files remain the source of truth. This module is kept
as the future home for import scripts if we move beyond local content files.
"""
